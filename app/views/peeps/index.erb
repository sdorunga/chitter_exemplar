<% @peeps.each do |peep| %>
  <div class="peep">
    <div class="content">
      <%= peep.content %>
    </div>
    <div class="timestamp">
      <%= date_and_time(peep.created_at) %>
    </div>
    <%= peep.user.name %>
  </div>
<% end %>
